# Backup and restore

Use `python scripts/backup.py --output reports/ares-backup.json` to create a metadata backup manifest.

Use `python scripts/restore.py reports/ares-backup.json` to validate a backup before restore.
